.DS_Store
__pycache__/
*.pyc
.ipynb_checkpoints/

# Cloned repo with its own git history — see conversation
nanoGPT/

# Datasets and trained model weights (regenerable, not source)
data/
models/*.pth

# Contains a certificate file
dockerfiles/
